# Core pipeline (required)
pydantic>=2.6

# Orchestration
langgraph>=0.2

# Structured LLM outputs + providers (real backends; optional at runtime —
# the system falls back to the mock backend when these are absent)
instructor>=1.3
openai>=1.30
anthropic>=0.30

# API
fastapi>=0.110
uvicorn[standard]>=0.29

# UI
streamlit>=1.33

# Tooling / tests
pytest>=8.0
httpx>=0.27
